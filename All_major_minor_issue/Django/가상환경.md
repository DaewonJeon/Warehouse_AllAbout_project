#### 가상환경 (venv) 설정 후 프로젝트 폴더 변경 
- 가상환경에 설정된 폴더 경로가 이름이 바뀌어서 인식을 못함
> 가상 환경 재설치

#### 가상환경 기본 명령어 모음
**Windows:**
```bash
python -m venv venv
.\venv\Scripts\activate

pip install -r requirements.txt

+ env  # 암호 있을시에만
+ python manage.py migrate
+ python manage.py createsuperuser
```
